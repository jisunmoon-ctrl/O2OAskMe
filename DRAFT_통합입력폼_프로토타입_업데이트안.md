# 통합 입력폼 · 프로토타입 업데이트안

> 작성: 2026-08-19
> 근거 문서: **통합 입력폼 퍼널 플로우 정책** (POLICY_FlowB A장 교체본)
> 대상 코드: `o2o_prototype_ods.html` 의 `o2hForm` 스텝
> 참조 Figma: 9470:79517 · 9470:79228 · 9470:79658 · 9478:117053 · 9445:86334
> 와이어 참조: `flowb_addr_funnel_wire.html`

> ⚠️ 근거 문서는 전달 과정에서 §0·§1·§3-1·§4·§5 본문 일부가 잘려 있었습니다. 잘린 구간의 규칙은 앞뒤 문맥과 다이어그램으로 복원해 반영했으며, 아래 **§7 확인이 필요한 것**에 복원 근거가 약한 항목을 따로 모았습니다.

---

## 1. 현행 프로토타입 진단

`o2hForm` 은 지금 **행 8개를 나열하는 입력 화면**입니다.

| 코드 | 현재 동작 | 정책 대비 |
|---|---|---|
| `VIEW.o2hForm` 타이틀 | `아래 정보만 입력하면 예상 견적을 알 수 있어요` | ❌ 입력 프레이밍. 정책 §0은 **확인 화면**으로 전환 |
| `o2hDetailRows()` | 시공 3행 + 이사 3행 + 청소 2행 = 최대 8행 | ❌ 정책 §2는 **블록 구성**, 파생값은 행으로 세우지 않음 |
| `평수` 행 | `o2hPrefillFromAddr()` 로 채우고도 **독립 행으로 노출** | ❌ 유추 가능한 값을 행으로 세우면 유저가 질문으로 인지 |
| `o2hPrefillAddrs()` | 배송지 → `caddr`(시공지) **검증 없이 직접 대입** | ❌ 정책 §3의 오염 주 경로. **역할 배정 1탭**이 선행돼야 함 |
| `S.ans['move.from'] = ['부산']` | 출발지를 하드코딩 prefill | ❌ 근거 없는 값. 정책은 **배송지 또는 직접 입력만** 소스로 인정 |
| `addr = [...caddr]` | 도착지 = 시공지 승계 | ✅ 정책과 일치 (실측 78.5%) |
| `claddr` 승계 | 시공지 → 없으면 도착지 | ✅ 정책과 일치 |
| `cdate`/`mdate`/`clday` 3행 | 각각 calendar 단일 선택 | ❌ 정책 §4는 **앵커 1개 + 시기 칩**, 청소 예정일 행 폐지 |
| `S.o2hLink = {}` | 일정 연동 체크박스 **기본 OFF** | ❌ 정책은 **기본 ON**(해제 2회를 요구하지 않음) |
| `o2hInfoReady()` | 모든 항목이 차야 CTA 활성 | ❌ 정책 §5는 **필수 항목 없음**. 「상담 후 결정」이 정식 응답 |
| 「상담 후 결정」 | Flow A 챗봇(`QMX`)에만 존재, `o2hForm` 에 없음 | ❌ 입력폼 전 항목에 일괄 도입 필요 |
| 층·엘리베이터 | `elev` 만 이사 선택 시 prefill, 폼에 행 없음 | ⚠️ 조회 성공은 일치. **실패 시 오더 카드 이월** 경로가 없음 |

---

## 2. 화면 재구성 — 행 8개 → 블록 3개

정책 §2의 "화면은 응답 지점이 아니라 블록으로 구성한다" 를 그대로 옮깁니다.

```
o2hSelect
   ↓
[블록 A] 주소            ← 응답 지점 ① (역할 배정 1탭 또는 주소 선택)
   ↓
[블록 B] 일정            ← 응답 지점 ② (앵커 시기 칩)
   ↓
[블록 C] 확인            ← 응답 지점 아님. 파생 결과 확인 + 편집 진입
   ↓
o2hOrder
```

**응답은 최대 2회.** 인터넷·렌탈만 선택했으면 블록 A·B를 모두 건너뛰고 바로 `o2hOrder` 로 보냅니다.

### 블록 A · 주소

Figma 4개 노드가 이 블록의 실제 형태입니다.

| 상태 | 화면 | Figma |
|---|---|---|
| 저장 주소 있음 | 라벨 우선 리스트(시공지·집·우리집·회사·본가) + 기본배송지 뱃지 | 9470:79517 |
| 검색 진입 | 풀높이 시트 + 검색어 형식 가이드 3종 | 9470:79228 |
| 검색 결과 | 우편번호 + 도로명(매칭어 하이라이트) + 지번 뱃지 | 9470:79658 |
| 상세·결손 | 상세주소 입력 → 조회 실패분만 인라인 등장 | 9478:117053 |

여기에 정책이 요구하는 것을 **추가**합니다.

1. **역할 배정 1탭** — 시공 + 이사를 동시 선택했을 때 폼 최상단에 노출.
   `이 주소(배송지)는 어떤 집인가요?` → `새로 들어갈 집` / `지금 사는 집` / `둘 다 아니에요`
   한 번의 응답으로 시공지·도착지·청소 장소·평수까지 최대 4개가 연쇄 파생됩니다.
2. **출발지 독립 수집** — `move.from` 은 배송지 또는 직접 입력만. `'부산'` 하드코딩 제거, 시공지·도착지에서 파생 금지 (실측 2.8%).
3. **주소 정규화** — 「○○와 동일」 표기 전에 도로명·지번 혼용을 흡수하는 정규화를 선행.
4. **도착지 「상담 후 결정」 + 시군구** — 미확정 도착지는 시군구 단위만 받습니다 (실측 95.3%).
5. **파생값은 행이 아니라 한 줄** — 평수·방 구조·층·엘베는 주소 아래 한 줄로 붙이고, **조회 실패분만** `입력하기` 로 남깁니다.

### 블록 B · 일정

**앵커 판정** — 시공 > 이사 > 청소 순으로 가장 앞선 서비스 하나에만 시기를 묻습니다.

**2단 정밀도** — 시기 칩(`이번 달` / `다음 달` / `2~3개월 뒤` / `상담 후 결정`)으로 먼저 받고, 정확한 날짜는 **선택 항목**으로 둡니다. 날짜를 건너뛰면 그 달의 최대 배수까지 상한만 개방합니다.

**연동 체크박스 기본 ON** — 현행은 기본 OFF라 체크 2회를 요구하고, 체크하지 않으면 필수 2개가 그대로 남습니다.

| 선택 조합 | 앵커 | 파생 | 기본 |
|---|---|---|---|
| 시공 + 이사 + 청소 | 시공 | 이사일 = 시공 완료 다음날 · 청소일 = 이사 다음날 | 둘 다 ON |
| 시공 + 청소 | 시공 | 청소일 = 시공 완료 +2일 | ON |
| 이사 + 청소 | 이사 | 청소일 = 이사 다음날 | ON |
| 단일 | 해당 서비스 | 없음 | — |

**청소 예정일 행 폐지** — 청소는 독립 시점을 갖지 않으므로 파생값 표기만 남깁니다.

### 블록 C · 확인

Figma 9445:86334 이 그대로 이 블록입니다 — 핀 타임라인 · 평수/층수/EV 3줄 · 편집하기 · 이전/확인하고 다음.

통합 폼용으로 **확장**이 필요합니다.

- Figma는 이사 단독(출발지·도착지 2핀)입니다. 통합 폼은 **시공지 · 이사 출발지 · 이사 도착지 · 청소 장소**를 조합에 따라 최대 4핀으로 늘려야 합니다.
- 승계된 주소는 별도 핀을 세우지 않고 **태그로 표기**합니다 (`도착지 · 시공지와 동일`). 정책 §3의 "행을 만들지 않고 태그로 표기".
- **가정 명시** — 「상담 후 결정」이 걸린 항목은 계산 가정을 문장으로 적습니다.
  예: `이사 갈 곳이 정해지지 않아 같은 구 안에서 이사한다고 보고 계산했어요`
- 일정 블록의 결과도 같은 화면에 요약합니다.

---

## 3. 상태·데이터 모델 변경

| 대상 | 변경 |
|---|---|
| `o2hPrefillAddrs()` | 배송지 → `caddr` 직접 대입 제거. **`S.o2hAddrRole` 응답 이후**에만 파생 실행 |
| 신규 `S.o2hAddrRole` | `'new'` (새 집) / `'current'` (지금 사는 집) / `'neither'` / `null`(미응답) |
| 신규 `o2hNormalizeAddr()` | 도로명·지번 혼용 정규화. 「동일」 표기와 비교의 전제 |
| `o2hPrefillFromAddr()` | 조회 성공/실패를 값과 함께 기록 → `{value, origin:'AUTO'|'USER'|'INHERIT', found:bool}` |
| `o2hDetailRows()` | **행 생성기 → 블록 생성기**로 교체. 파생값은 행을 만들지 않음 |
| 신규 `o2hScheduleAnchor()` | 시공 > 이사 > 청소 순 앵커 판정 |
| 신규 `S.o2hPeriodChip` | 시기 칩 값. 정확 날짜(`cdate` 등)는 선택 |
| `S.o2hLink` | 초기값을 조합별 기본 ON 으로 (`defaultO2hLink(svcs)`) |
| `o2hInfoReady()` | **응답 지점 2개가 응답됐는지**만 검사. 「상담 후 결정」도 응답으로 인정 |
| 신규 `S.o2hDeferred` | 「상담 후 결정」 선택 항목 집합. 견적 **정확도에는 미반영**, 상담 회수용으로 표시 |
| 층·엘베 실패분 | 폼에 남기지 않고 **오더 카드로 이월** (`O2H_EXTRA` 경로 재사용) |

---

## 4. Figma 5개 노드가 덮는 범위와 갭

| 정책 요구 | Figma | 상태 |
|---|---|---|
| 확인 화면 컨셉 | 9445:86334 | ✅ 그대로 채택 |
| 주소 리스트 · 검색 · 상세 | 9470:79517 / 79228 / 79658 / 9478:117053 | ✅ 그대로 채택 |
| 조회 실패분만 입력 | 9478:117053 (평수·층수 인라인) | ✅ 일치 |
| 층·EV 실패 시 표기 | 9445:86334 의 `정보 없음` | ✅ 일치 |
| **역할 배정 1탭** | — | ❌ **Figma 없음** — 신규 설계 필요 |
| **일정 시기 칩** | — | ❌ **Figma 없음** — 신규 설계 필요 |
| **「상담 후 결정」** | — | ❌ **Figma 없음**. 9478:117053 은 평수가 `*` 필수라 **정책과 충돌** |
| **도착지 시군구 단위 수집** | — | ❌ Figma 없음 |
| **다핀 확인 화면(최대 4핀)** | 2핀만 | ⚠️ 확장 필요 |
| **가정 명시 문구** | — | ❌ Figma 없음 |

> **가장 큰 충돌** — Figma 9478:117053 은 평수를 필수(`*`)로 두고 미충족 시 `확인` 을 잠급니다. 정책 §5는 입력폼에 필수 항목을 두지 않고 「상담 후 결정」으로 통과시킵니다. **평수 칩에 「상담 후 결정」을 추가하고 CTA 잠금을 푸는 방향**을 제안하지만, 견적 산출이 평수 없이 성립하는지 확인이 필요합니다.

---

## 5. 적용 순서

**1단계 — 프레이밍과 파생 오염 차단 (P0)**
1. `o2hForm` 타이틀을 확인 프레이밍으로 교체
2. `o2hPrefillAddrs()` 의 배송지 → 시공지 무검증 대입 제거
3. 역할 배정 1탭 추가 (`S.o2hAddrRole`)
4. `move.from` 하드코딩 `'부산'` 제거
5. 평수를 독립 행에서 빼고 주소 아래 한 줄로 이동

**2단계 — 응답 지점 2개로 축소 (P0)**
6. 일정 3행 → 앵커 1개 + 시기 칩
7. 청소 예정일 행 폐지
8. `S.o2hLink` 기본 ON
9. 전 항목에 「상담 후 결정」 도입 + `o2hInfoReady()` 완화

**3단계 — 확인 화면 (P1)**
10. Figma 9445:86334 구조를 `o2hForm` 마지막 블록으로 이식
11. 최대 4핀으로 확장, 승계 주소는 태그 표기
12. 가정 명시 문구 + 상담 회수 표시

**4단계 — 정합성 (P1)**
13. 주소 정규화 함수 도입
14. 층·엘베 실패분 오더 카드 이월
15. 도착지 시군구 단위 수집

---

## 6. 이 변경으로 줄어드는 것

| | 현행 | 제안 |
|---|---|---|
| 시공+이사+청소 선택 시 행 | 8행 | 응답 지점 2개 |
| 필수 항목 | 8개 | 0개 (「상담 후 결정」이 정식 응답) |
| 배송지 오염 경로 | 무검증 대입 | 역할 배정 1탭으로 차단 |
| 일정 체크 조작 | 해제 2회 | 기본 ON, 필요할 때만 해제 |

---

## 7. 확인이 필요한 것

1. **평수 필수 여부** — 정책(필수 없음)과 Figma(평수 `*` 필수)가 충돌합니다. 평수 없이 견적 범위가 산출되는지 확인 필요.
2. **역할 배정 문구** — 원문에서 선택지가 `새로 들어갈 집` / `지금 사는 집`(추정) / `둘 다 아니에요` 로 잘려 있었습니다. 확정 카피 필요.
3. **`시공 + 청소` 청소일 파생** — 잘린 구간에서 `시공 완료 +2일` 로 복원했습니다. 검증 필요.
4. **시기 칩 4종 라벨** — Flow A 챗봇은 `이번 달 안 (D-15)` / `다음 달 (D-40)` / `2~3개월 뒤 (D-75)` / `아직 미정` 을 씁니다. 입력폼도 D-day 표기를 붙일지, `상담 후 결정` 으로 통일할지 결정 필요.
5. **상담 회수 표시 위치** — 파트너에게 미확정 항목을 전달한다고만 되어 있습니다. 오더 카드에 표시할지 신청 단계에 표시할지 미정.
6. **층·엘베 이월 시점** — 오더 카드로 이월한다고 되어 있으나, 이사 가산(3~9층 +12만 / 10~19층 +15만 / 20층~ +26.5만)이 견적 범위에 직접 들어가므로 이월 시 범위 표기 방식 확인 필요.
